<%# Success Toast Component %>
<%# Displays success notification with green checkmark icon %>
<%# Auto-dismisses after 5 seconds, can be manually dismissed %>

<div 
  data-controller="toast"
  data-toast-type-value="notice"
  data-toast-timeout-value="5000"
  class="flex items-center gap-3 w-full max-w-sm rounded-lg bg-white shadow-lg ring-1 ring-black ring-opacity-5 border-l-4 border-green-500 p-4 transition-all duration-300"
  role="alert"
  aria-live="polite">
  <%# Success Icon %>
  <div class="flex-shrink-0">
    <svg class="h-5 w-5 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
  </div>
  
  <%# Message Text %>
  <p class="flex-1 text-sm font-medium text-gray-900"><%= message %></p>
  
  <%# Close Button %>
  <button
    type="button"
    data-action="click->toast#dismiss"
    class="flex-shrink-0 p-1 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-green-500"
    aria-label="Dismiss notification">
    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
    </svg>
  </button>
</div>

